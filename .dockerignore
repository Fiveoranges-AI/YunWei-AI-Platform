# Keep the build context lean — exclude large dirs not needed by platform/Dockerfile.
# Stage 1a/1b (node) copy app/ and app-win/ package*.json + src; node_modules rebuilt via npm ci.
# Stage 2 (python) copies platform/pyproject.toml + platform/platform_app + platform/yinhu_brain + platform/migrations + platform/static.

**/node_modules/
**/dist/
.git/
.venv/
__pycache__/
*.pyc
.pytest_cache/
landing/
agents/
ops/
docs/
